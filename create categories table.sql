CREATE TABLE
    Categories (
        `CategoryID` INT PRIMARY KEY,
        `Name` VARCHAR(255)
    );

INSERT INTO
    Categories (CategoryID, Name)
VALUES (301, 'Category X'), (302, 'Category Y'), (303, 'Category Z'), (304, 'Category A'), (305, 'Category B');